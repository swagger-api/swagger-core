<?xml version="1.0" encoding="UTF-8"?>
<ivysettings>
    <settings defaultResolver="chained">
        <credentials host="ci.wordnik.com" username="mavenuser" passwd="m4v3np455" realm="Wordnik CI Server"/>
    </settings>

	<property name="ivy.checksums" value=""/>
	<property name="ivy.local.default.root" value="${ivy.default.ivy.user.dir}/local"
		override="false" />
	<property name="ivy.local.default.ivy.pattern"
		value="[organisation]/[module]/[revision]/[type]s/[artifact].[ext]"
		override="false" />
	<property name="ivy.local.default.artifact.pattern"
		value="[organisation]/[module]/[revision]/[type]s/[artifact].[ext]"
		override="false" />
   	<property name="maven.pattern"
              value="[organisation]/[module]/[revision]/[artifact]-[revision].[ext]"
              override="false" />
	<resolvers>
        <!-- chain name="chained" returnFirst="true">
                <filesystem name="local">
                    <ivy pattern="${ivy.local.default.root}/${ivy.local.default.ivy.pattern}" />
                    <artifact pattern="${ivy.local.default.root}/${ivy.local.default.artifact.pattern}" />
                </filesystem>
                <ibiblio name="wordnik-repo" root="https://ci.wordnik.com/artifactory/libs-snapshots" m2compatible="true" pattern="${maven.pattern}"/>
                <ibiblio name="wordnik-remote-repos" root="https://ci.wordnik.com/artifactory/remote-repos" m2compatible="true"/>
        </chain -->
		<chain name="chained" returnFirst="true">
			<filesystem name="local">
				<ivy pattern="${ivy.local.default.root}/${ivy.local.default.ivy.pattern}" />
				<artifact pattern="${ivy.local.default.root}/${ivy.local.default.artifact.pattern}" />
			</filesystem>
			<ibiblio name="ibiblio" m2compatible="true" />
			<ibiblio name="javanet" root="http://download.java.net/maven/2/" m2compatible="true" />
		</chain>
	</resolvers>
</ivysettings>
