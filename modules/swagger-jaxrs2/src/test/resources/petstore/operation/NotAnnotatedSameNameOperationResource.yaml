openapi: 3.0.0
paths:
  /notannotatedoperation:
    get:
      operationId: getUser
      responses:
        default:
          description: default response
          content:
            '*/*':
              schema:
                type: string
  /notannotatedoperationduplicated:
    get:
      operationId: getUser
      requestBody:
        content:
          '*/*':
            schema:
              type: string
      responses:
        default:
          description: default response
          content:
            '*/*':
              schema:
                type: string
