openapi: 3.0.0
paths:
  /operationwithouannotation:
    get:
      operationId: getUser
      responses:
        default:
          description: default response
          content:
            '*/*':
              schema:
                type: string
